//code//
var count = 0;
onmessage = handleMessage;

function handleMessage(evt) {
    var data = evt.data;
    if (data.message = "ping") {
        sendMessage();
    }
}

function sendMessage() {
    postMessage( { message: "pong", count: count } );
    count++;
    setTimeout("sendMessage()", 2500);
}