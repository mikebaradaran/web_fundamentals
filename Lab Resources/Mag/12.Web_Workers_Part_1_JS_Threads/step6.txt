//code//
var aWorker = new Worker("worker.js");
aWorker.onmessage = onWorkerMessage;

function onWorkerMessage(evt) {
    var data = evt.data;
    document.getElementById("result").innerHTML = "Worker said " + data;
}